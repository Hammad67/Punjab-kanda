<h1>Send Messages</h1>
<table class="table table-dark table-bordered">
  <thead>
  <tr>
    <th>Serialnumber</th>
    <th>Vehiclenumber</th>
    <th>Product</th>
    <th>Weighttype</th>
    <th>BeforeloadWeight</th>
    <th>AfterloadWeight</th>
    <th>Netweight</th>
    <th>Cash</th>
    <th>Party</th>
    <th>Driver</th>
    <th colspan="3">Actions</th>
  </tr>
  </thead>

  <tbody>
  <% @send_messages.each do |send_message| %>
    <tr>
      <td><%= send_message.srial_number %></td>
      <td><%= send_message.vehicle_number %></td>
      <td><%= send_message.product %></td>
      <td><%= send_message.weight_type %></td>
      <td><%= send_message.before_load_weight %></td>
      <td><%= send_message.after_load_weight %></td>
      <td><%= send_message.net_weight %></td>
      <td><%= send_message.cash %></td>
      <td><%= Party.find_by(@send_message.party_id).name %></td>
      <td><%= Driver.find_by(@send_message.driver_id).name %></td>
      <td><%= link_to 'Show', send_message, class: "btn btn-info" %></td>
      <td><%= link_to 'Destroy', send_message, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Send Message', new_send_message_path, class: "btn btn-success" %>
